<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardCornerRadius="8dp"
    card_view:cardElevation="4dp"
    card_view:cardBackgroundColor="#1E1E1E"> <!-- Dark background for the card -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="#6C6565"> <!-- Matching dark background for the LinearLayout -->

        <Button
            android:id="@+id/btnDetalleRestaurante"
            style="@style/Widget.Material3.ExtendedFloatingActionButton.Icon.Secondary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="7dp"
            android:layout_weight="1"
            android:drawableLeft="@drawable/baseline_dining_24"
            android:drawablePadding="8dp"
            android:text="Detalle Alojamiento"
            android:textColor="#FFFEFE"
            android:backgroundTint="#333333"
            android:iconTint="@color/black" />

    </LinearLayout>

</androidx.cardview.widget.CardView>

